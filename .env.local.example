# Optional — enables AI distillation with Gemini.
# Get a key at https://aistudio.google.com/apikey then copy this file to .env.local.
# Without a key the app still runs; Distill uses the built-in offline summarizer.
GEMINI_API_KEY=
